import SimpleKeyStore from '../BackEnd/SimpleKeyStore';

it('indexedDB is available', () => {
  // console.log(global.localstorage)
  // console.log(global.indexedDB.open("dbName", 1))
});

it('basic workflow works', () => {
  // const openreq = indexedDB.open("dbName", 1);
  // let sks = new SimpleKeyStore("test-db", "test-store")
  // const aaa_val = [1, 2, 3, 4, 5];
  // const bbb_val = "test a b c d e";
  // sks.set("aaa", aaa_val)
  // sks.set("bbb", bbb_val)
  // expect(sks.get("aaa")).toEqual(aaa_val)
  // expect(sks.get("bbbx")).toEqual(bbb_val)
});

